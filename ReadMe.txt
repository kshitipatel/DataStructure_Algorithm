Important functions =====>

Arrays.sort(nums);  //Sort array in asc order
Arrays.sort(array, Collections.reverseOrder()); // Sorting the array in descending order
Arrays.toString(array); //Array to string for printing
Math.max(a, b);   //Maximum of 2 numbers
Math.min(a, b);   //Minimum of 2 numbers
